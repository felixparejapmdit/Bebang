/* App version — shared by the page and the service worker (sw.js imports this file).
   Bump it on every deploy: installed apps then show "A new version is available → Reload". */
const APP_VERSION = '2026.5.1';
